{{- if .Values.ingress.ws.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "aetherium-manifest.fullname" . }}-ws
  labels:
    {{- include "aetherium-manifest.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.ingress.ws.annotations | nindent 4 }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.ws.host | quote }}
      http:
        paths:
          - path: {{ .Values.ingress.ws.path | quote }}
            pathType: {{ .Values.ingress.ws.pathType }}
            backend:
              service:
                name: ws-gateway
                port:
                  number: {{ .Values.ingress.ws.servicePort }}
  {{- if .Values.ingress.ws.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.ws.host | quote }}
      secretName: {{ .Values.ingress.ws.tls.secretName | quote }}
  {{- end }}
{{- end }}
